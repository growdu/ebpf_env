global:
  scrape_interval: 15s
  scrape_timeout: 10s

scrape_configs:
  - job_name: 'prometheus'
    static_configs:
      - targets: ['localhost:9090']

  - job_name: 'ebpf_exporter'
    metrics_path: /metrics
    static_configs:
      - targets: ['ebpf-dev:9100']
